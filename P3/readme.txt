client: (forwards commands to the server)
    can connect OUR server to another: example commmand -> connect <ip><port>

server:
    has responses of its own that our client has no control over
    if another server wants to connect to our server, thats fine, our client doesnt need to know that

